[project]
name = "wiki-search"
version = "0.1.0"
description = "A wikipedia dump processing pipeline"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.7",
]

[project.optional-dependencies]
perf = [
  "orjson>=3.10.0",
  "lxml>=5.2.0",
]
